name: syria_car_care2
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.3
  supabase_flutter: ^2.0.0
  get_it: ^7.6.4
  equatable: ^2.0.5
  dartz: ^0.10.1
  flutter_map: ^6.0.1
  latlong2: ^0.9.0
  geolocator: ^10.1.0
  google_fonts: ^6.1.0
  intl: ^0.20.2
  easy_localization: ^3.0.3
  image_picker: ^1.2.2
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/translations/
    - assets/images/

